                             ----------
Socket Pasivo (SP)--- ====== | Server |
                  |          ----------
                  V
               ---------
Client (C) ====| Proxy |==== Origin (O)
               ---------

---> |write_buffer| --->
<--- |read_buffer | <---

1. Parsers
   a. Socks5 Parser: hello, addres, user, password

   b. Admin Parser: user, password, config, logs

   c. cmd parser

2. Proxy TCP (origin: 127.0.0.1 9090)
   a. crear socket pasivo
   (SP) READ si tengo capacidad para atender nuevos clientes

   b. aceptar la conexión (accept)

   c. (C) READ_HELLO

   d. (C) WRITE_HELLO

   e. (C) READ_REQUEST

   f. (C) GET_ADDR - getaddrinfo

   g. (C) CONNECT - creo socket activo a Origin - configurar no bloqueante
   connect(127.0.0.1 9090)
   subscribir(WRITE)
   write -> check estado de la conexión

   h. WRITE_REQUEST

   i. COPY

Read socket(C) si write_buffer tiene capacidad
Read socket(O) si read_buffer tiene capacidad
Write socket(C) si read_buffer tiene capacidad
Write socket(O) si write_buffer tiene capacidad

3. Aplicación Cliente
   a. procesar los comandos de entrada

   b. establecer la conexión con el sv

   c. comunicarse con el server
